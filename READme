#  Password Strength Checker

This Python program checks the strength of a user's password based on various criteria like length, use of uppercase letters, lowercase letters, numbers, and special characters.

---

##  Features

- Checks if the password:
  - Has at least 8 characters
  - Contains uppercase letters
  - Contains lowercase letters
  - Includes numbers
  - Contains special characters
- Categorizes password strength as:
  - **Weak**
  - **Medium**
  - **Strong**

---

##  Requirements

- Python 3.x

---

##  How to Run

1. Clone the repository or download the Python file.

2. Open your terminal and navigate to the folder containing the file.

3. Run the program using:

   ```bash
   python password_checker.py
